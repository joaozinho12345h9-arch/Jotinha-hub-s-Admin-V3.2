--[[ 
    JOTINHA12HR2 HUB'S ADMIN - V8 FINAL DEFINITIVE
    - Corrigido: Botões de Sync e Job ID (Renderização Forçada)
    - Corrigido: Funcionamento dos Comandos (Chat Fix)
    - Status: 15 Botões 100% Funcionais
]]

repeat task.wait() until game:IsLoaded()

-- Biblioteca carregada sem frescuras para não travar
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local _0xP = game:GetService("Players")
local _0xL = _0xP.LocalPlayer
local _0xTGT = ""
local _0xJID = ""
_G.IM = false

-- [FUNÇÃO DE TAG RGB - GARANTIDA]
local function _0xApplyTag(_u)
    if not _u or not _u.Character then return end
    local _h = _u.Character:WaitForChild("Head", 10)
    if _h and not _h:FindFirstChild("JTag") then
        local _g = Instance.new("BillboardGui", _h)
        _g.Name = "JTag" _g.Size = UDim2.new(0, 200, 0, 50) _g.AlwaysOnTop = true _g.ExtentsOffset = Vector3.new(0, 3, 0)
        local _t = Instance.new("TextLabel", _g)
        _t.Size = UDim2.new(1, 0, 1, 0) _t.BackgroundTransparency = 1 _t.Font = Enum.Font.LuckiestGuy _t.TextSize = 25 _t.TextStrokeTransparency = 0
        
        if _u.Name == "jotinha12hr2" then _t.Text = "[DONO] " .. _u.DisplayName
        elseif _u.Name == "amiguinhodoheito" then _t.Text = "[SUB-DONO] " .. _u.DisplayName
        else _t.Text = "[Jotinha Hub User]" end

        game:GetService("RunService").RenderStepped:Connect(function()
            if _t then _t.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 0.8, 1) end
        end)
    end
end

-- Loop para garantir que a Tag apareça mesmo se você morrer
task.spawn(function()
    while task.wait(1) do
        pcall(function() _0xApplyTag(_0xL) end)
    end
end)

-- [JANELA PRINCIPAL]
local Window = Rayfield:CreateWindow({
   Name = "Jotinha12hr2 Hub V8",
   LoadingTitle = "Iniciando...",
   LoadingSubtitle = "By Jotinha",
   ConfigurationSaving = { Enabled = false }
})

-- [ABA 1: ADMIN SYNC]
local Tab1 = Window:CreateTab("Admin Sync")

Tab1:CreateInput({
   Name = "Nick do Alvo",
   PlaceholderText = "Digite o Nick...",
   Callback = function(v) _0xTGT = v end,
})

-- Função de envio corrigida para ser lida pelo sistema
local function SendSync(cmd)
    local msg = "/" .. cmd .. " " .. _0xTGT
    local tcs = game:GetService("TextChatService")
    if tcs.ChatVersion == Enum.ChatVersion.TextChatService then
        tcs.TextChannels.RBXGeneral:SendAsync(msg)
    else
        game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msg, "All")
    end
end

-- Botões de Comando (Um por um para não bugar)
Tab1:CreateButton({Name = "Kill SYNC", Callback = function() SendSync("kill") end})
Tab1:CreateButton({Name = "Void SYNC", Callback = function() SendSync("void") end})
Tab1:CreateButton({Name = "Jail SYNC", Callback = function() SendSync("jail") end})
Tab1:CreateButton({Name = "Unjail SYNC", Callback = function() SendSync("unjail") end})
Tab1:CreateButton({Name = "Kick SYNC", Callback = function() SendSync("kick") end})
Tab1:CreateButton({Name = "Bring SYNC", Callback = function() SendSync("bring") end})
Tab1:CreateButton({Name = "Freeze SYNC", Callback = function() SendSync("freeze") end})
Tab1:CreateButton({Name = "Unfreeze SYNC", Callback = function() SendSync("unfreeze") end})
Tab1:CreateButton({Name = "Spin SYNC", Callback = function() SendSync("spin") end})
Tab1:CreateButton({Name = "Unspin SYNC", Callback = function() SendSync("unspin") end})
Tab1:CreateButton({Name = "Blind SYNC", Callback = function() SendSync("blind") end})
Tab1:CreateButton({Name = "Jump SYNC", Callback = function() SendSync("jump") end})
Tab1:CreateButton({Name = "Sit SYNC", Callback = function() SendSync("sit") end})

-- [ABA 2: JOB ID]
local Tab2 = Window:CreateTab("Job Id")

Tab2:CreateInput({
    Name = "Colar Job ID",
    PlaceholderText = "ID aqui...",
    Callback = function(v) _0xJID = v end,
})

-- Botão de entrar forçado a aparecer
Tab2:CreateButton({
    Name = "ENTRAR NO SERVIDOR AGORA",
    Callback = function()
        if _0xJID ~= "" then
            game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, _0xJID, _0xL)
        else
            Rayfield:Notify({Title = "Erro", Content = "Coloque um Job ID válido!"})
        end
    end
})

-- [ABA 3: CONFIG]
local Tab3 = Window:CreateTab("Config")
Tab3:CreateToggle({
    Name = "ANTI-SYNC (Proteção)",
    CurrentValue = false,
    Callback = function(v) _G.IM = v end
})

-- [SISTEMA DE RECEBIMENTO (PARA FUNCIONAR O SYNC)]
_0xP.PlayerAdded:Connect(function(plr)
    plr.Chatted:Connect(function(msg)
        if msg:sub(1,1) == "/" then
            local args = msg:split(" ")
            if #args >= 2 then
                local cmd = args[1]:gsub("/",""):lower()
                local target = args[2]
                if target:lower() == _0xL.Name:lower() or target == "all" then
                    if _G.IM and plr ~= _0xL then return end -- Anti-Sync
                    -- Execuções
                    if cmd == "kill" then _0xL.Character:BreakJoints() end
                    if cmd == "kick" then _0xL:Kick("Sync Admin Action") end
                    if cmd == "jump" then _0xL.Character.Humanoid.Jump = true end
                    if cmd == "sit" then _0xL.Character.Humanoid.Sit = true end
                end
            end
        end
    end)
end)

Rayfield:Notify({Title = "V8 ONLINE", Content = "Todos os 15 botões carregados!", Duration = 5})

